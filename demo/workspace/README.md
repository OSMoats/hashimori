# acme-webhooks (demo workspace)

A deliberately tiny project for recording the Hashimori runtime demo.
`tests/test_app.py::test_discount_never_negative` fails on purpose.
`.env` contains FAKE credentials. `build/` is a throwaway artifact.
